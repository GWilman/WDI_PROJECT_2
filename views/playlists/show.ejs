<h1>Playlist</h1>
<div class="col-12">
  <div class="row">
      <% user.userPlaylist.forEach(post => { %>
      <%= post._id %>
      <% const videoID = youtubeParser(post.videoURL); %>
      <iframe class="wall-post-vid" src="https://www.youtube.com/embed/<%= videoID %>" frameborder="0" allowfullscreen></iframe>
      <% }) %>
  </div>
</div>
